---
// Basic server-side redirects for legacy Jekyll-style paths if hosted with adapters that support SSR
const redirects: Array<{ from: string; to: string; status?: number }> = [];
// Example: redirects.push({ from: '/Blog/:slug/', to: '/?types=blog' });
export const prerender = true;
---

<html>
  <head>
    <meta http-equiv="refresh" content="0; url=/" />
  </head>
  <body>
    <a href="/">Redirecting…</a>
  </body>
</html>

