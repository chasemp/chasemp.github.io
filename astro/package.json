{
  "name": "timeline-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch:readwise": "node --experimental-strip-types scripts/fetch-readwise.mts",
    "fetch:blog": "node --experimental-strip-types scripts/fetch-blog.mts",
    "merge": "node --experimental-strip-types scripts/merge-sources.mts",
    "generate": "npm run fetch:readwise && npm run fetch:blog && npm run merge"
  },
  "dependencies": {
    "astro": "^4.14.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.6.2"
  }
}

